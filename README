Kernel IB Pinpong test
	This project is a kernel module implementation of IP pingpong test.
	The purpose of this module is to execute RDMA and send/recv verbs
	between two peers.

Prerequisites
	Install kernel-devel package

Installing
	Download source and run make (make modules_install was not tested)

Running
	Load the module with the module params.
	cycles: Number of desired execution cycles
	server: Set to 1 to run as a sender-side
	receiver: Set to 1 to run as a receiver-side
	dguid: Peer's GUID

	Check output with dmesg

Author
	Yuval Shaia <yuval.shaia@oracle.com>

License
	See license file
